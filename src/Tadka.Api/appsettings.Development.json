{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    // Local Docker Postgres only. Dummy password, committed on purpose (throwaway DB, no real data).
    // This is NOT how production is configured.
    // Override (wins over this file): User Secrets in Development, or env ConnectionStrings__TadkaDb.
    // When the password is real it never lives in git. Day 10 / deploy.
    "TadkaDb": "Host=localhost;Port=5432;Database=tadka;Username=tadka;Password=tadka_local"
  }
}
